{% extends "front/base.html.twig" %}
   
{% block content %}
    
    <section>
       <article class="container">
        <div class="row">
			<div class="col-xs-12"><h2>Réinitialisation de mot de passe - Nouveau mot de passe</h2></div>
		</div>
        {% for info in app.session.flashbag.get('info') %}
            <div class="alert alert-notice">
                {{ info }}
            </div>
        {% endfor %}
        <br/>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <br/>
        <button type="submit" class="btn btn-default">Enregistrer le nouveau mot de passe</button>
        {{ form_end(form) }}
        </article>

    </section>


{% endblock %}